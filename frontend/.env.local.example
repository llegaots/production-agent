# Frontend reads repo-root ../.env via next.config.ts (SUPABASE_URL + SUPABASE_SERVICE_KEY).
# Optional overrides only — do not duplicate Supabase keys here unless you need local overrides.
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
