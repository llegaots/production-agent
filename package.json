{
  "name": "production-agent",
  "version": "0.1.0",
  "description": "Multi-agent production week planner for window cleaning and field service businesses",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "plan": "npm run build && node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  }
}
