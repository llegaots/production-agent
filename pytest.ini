[pytest]
markers =
    e2e: orchestrator end-to-end tests (Supabase isolated fixtures)
    llm: tests that call Anthropic (skip in CI unless RUN_LLM_E2E=1)
    anthropic: legacy marker for live Anthropic tests
testpaths = tests
pythonpath = backend
